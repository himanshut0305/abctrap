<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/login_activity_layout"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/login_background_image"
        android:scaleType="centerCrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/login_background_orange"/>

    <LinearLayout
        android:orientation="vertical"
        android:background="#bb000000"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/school_logo_image"
            android:layout_marginTop="75dp"
            android:layout_gravity="center_horizontal"
            android:layout_width="wrap_content"
            android:layout_height="150dp"
            android:src="@drawable/school_logo_qps"/>

        <TextView
            android:id="@+id/school_name_label"
            android:layout_marginTop="45dp"
            android:layout_gravity="center_horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="QED Public School"
            android:textColor="#fff"
            android:fontFamily="sans-serif-condensed"
            android:textSize="22sp"/>

        <LinearLayout
            android:paddingTop="45dp"
            android:paddingRight="30dp"
            android:paddingLeft="30dp"
            android:paddingBottom="30dp"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <EditText
                android:id="@+id/username"
                android:hint="Username"
                android:textColor="@color/white"
                android:textColorHint="#999999"
                android:drawablePadding="10dp"
                android:paddingBottom="20dp"
                android:singleLine="true"
                android:drawableLeft="@drawable/icon_login_username"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <EditText
                android:id="@+id/password"
                android:hint="Password"
                android:textColor="@color/white"
                android:textColorHint="#999999"
                android:drawablePadding="10dp"
                android:paddingBottom="20dp"
                android:singleLine="true"
                android:drawableLeft="@drawable/icon_login_password"
                android:inputType="textPassword"
                android:layout_marginTop="5dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

        </LinearLayout>

        <Button
            android:id="@+id/login_button"
            android:text="Login"
            android:textColor="#FFF"
            android:background="@drawable/custom_button"
            android:layout_marginRight="30dp"
            android:layout_marginLeft="30dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="onLoginClick" />

        <TextView
            android:layout_marginTop="30dp"
            android:textColor="#999"
            android:text="Learn Better"
            android:layout_gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="onChangeSchoolClick" />

    </LinearLayout>

</FrameLayout>